using UnityEngine;

public class CameraFollow : MonoBehaviour
{
    //¬ообще скрипт управл€ет не камерой, а точкой, за которой следует камера

    [SerializeField] private Transform _cameraPivot;

    //ѕусть просто стабилизирует эту точку относитльно мировых координат ()
}
